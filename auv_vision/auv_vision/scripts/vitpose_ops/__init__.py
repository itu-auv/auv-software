# Operations for vitpose_process_node: one module per op type, each exporting
# create_op(params, ctx). The op contract is in vitpose_process_node.py's
# docstring; gate_pose.py and tetra_unfold.py are the worked examples.
