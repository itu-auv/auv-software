#!/usr/bin/env python
PACKAGE = "auv_mapping"

from dynamic_reconfigure.parameter_generator_catkin import *

gen = ParameterGenerator()

gen.add(
    "gate_closer_distance",
    double_t,
    0,
    "Distance of gate_closer from gate_link along its local -Y axis (m)",
    1.0,
    0.0,
    10.0,
)
gen.add(
    "gate_farther_distance",
    double_t,
    0,
    "Distance of gate_farther from gate_link along its local +Y axis (m)",
    2.5,
    0.0,
    10.0,
)

exit(gen.generate(PACKAGE, "auv_mapping", "PingerTeknofestTrajectory"))
