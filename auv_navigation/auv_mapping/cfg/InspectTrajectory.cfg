#!/usr/bin/env python
PACKAGE = "auv_mapping"

from dynamic_reconfigure.parameter_generator_catkin import *

gen = ParameterGenerator()

gen.add(
    "long_side_extension",
    double_t,
    0,
    "Amount added to the rectangle's long side (m). Short side scales proportionally.",
    2.0,
    0.0,
    5.0,
)

gen.add(
    "height_above_valve",
    double_t,
    0,
    "Height (m) above the valve's Z at which all inspect frames are placed.",
    0.0,
    0.0,
    5.0,
)

exit(gen.generate(PACKAGE, "auv_mapping", "InspectTrajectory"))
