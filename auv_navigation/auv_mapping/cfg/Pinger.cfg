#!/usr/bin/env python
PACKAGE = "auv_mapping"
from dynamic_reconfigure.parameter_generator_catkin import *

gen = ParameterGenerator()
gen.add("search_distance", double_t, 0, "Search distance for waypoint offsets", 2.0, 0.0, 3.0)

exit(gen.generate(PACKAGE, "auv_mapping", "Pinger"))
