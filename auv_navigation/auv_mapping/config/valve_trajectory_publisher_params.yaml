# Valve trajectory publisher default parameters
coarse_approach_offset: 2.50  # Coarse approach frame distance (m), robot->valve direction
approach_offset: 2.00         # Oriented approach frame distance (m), surface normal — yaw alignment here
contact_offset: 0.625        # Contact frame distance (m), surface normal — valve-guided final approach
